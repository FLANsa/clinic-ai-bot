# إصلاح مشكلة GROQ_API_KEY

## المشكلة
```
❌ اتصال LLM
فشل الاتصال بـ LLM: Error code: 401 - Invalid API Key
```

## الحل

### 1. الحصول على مفتاح Groq API جديد

1. اذهب إلى: https://console.groq.com/
2. سجّل الدخول بحسابك
3. اذهب إلى **API Keys**
4. اضغط **Create API Key**
5. انسخ المفتاح الجديد

### 2. إضافة المفتاح في Render Dashboard

1. اذهب إلى Render Dashboard
2. اختر Service الخاص بالبوت
3. اضغط **Environment** من القائمة الجانبية
4. ابحث عن `GROQ_API_KEY`
5. إذا كان موجوداً:
   - اضغط **Edit**
   - الصق المفتاح الجديد
   - اضغط **Save Changes**
6. إذا لم يكن موجوداً:
   - اضغط **Add Environment Variable**
   - **Key**: `GROQ_API_KEY`
   - **Value**: الصق المفتاح الجديد
   - اضغط **Save Changes**

### 3. إعادة نشر الخدمة

بعد إضافة المفتاح:
- Render سيعيد نشر الخدمة تلقائياً
- أو اضغط **Manual Deploy** > **Deploy latest commit**

### 4. التحقق من الإصلاح

1. اذهب إلى صفحة Test Chat
2. اضغط **🔍 تشخيص المشاكل**
3. تحقق من أن **اتصال LLM** أصبح ✅

---

## ملاحظات مهمة

- ⚠️ المفتاح يجب أن يبدأ بـ `gsk_`
- ⚠️ لا تشارك المفتاح مع أحد
- ⚠️ إذا انتهت صلاحية المفتاح، أنشئ واحداً جديداً

