# 🧪 اختبار الشات بوت محلياً

## تشغيل النظام محلياً

### 1. تشغيل الباك إند (البورت 8000)

```bash
./start-backend.sh
```

أو يدوياً:
```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

الباك إند سيعمل على: `http://localhost:8000`

### 2. تشغيل الفرونت إند (البورت 3000)

في terminal جديد:
```bash
./start-frontend.sh
```

أو يدوياً:
```bash
cd frontend
npm run dev
```

الفرونت إند سيعمل على: `http://localhost:3000`

### 3. فتح صفحة الاختبار

افتح المتصفح واذهب إلى:
```
http://localhost:3000/test-chat
```

## المميزات

- ✅ واجهة حديثة وسهلة الاستخدام
- ✅ عرض النية المكتشفة لكل رسالة
- ✅ عرض metadata (RAG, unrecognized, needs_handoff)
- ✅ مسح المحادثة بسهولة
- ✅ أمثلة للأسئلة

## متطلبات

- Python 3.9+ مع virtual environment
- Node.js 18+ مع npm
- قاعدة بيانات PostgreSQL قيد التشغيل

## ملاحظات

- تأكد من تشغيل قاعدة البيانات أولاً (Docker Compose أو PostgreSQL محلي)
- تأكد من وجود ملف `.env` في مجلد `backend` مع الإعدادات المطلوبة
- الباك إند يستخدم البورت 8000 والفرونت إند يستخدم البورت 3000

